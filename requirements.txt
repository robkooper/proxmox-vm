# Proxmox VM Management Dependencies

# Proxmox API wrapper
proxmoxer>=2.0.0

# HTTPS backend for proxmoxer
requests>=2.31.0

# For large file uploads (prevents high memory usage)
requests-toolbelt>=1.0.0

# YAML parsing for cloud-init configuration
PyYAML>=6.0.0

# ISO file creation for cloud-init
pycdlib>=1.14.0

# NetBox API client for IP and DNS management
pynetbox>=7.0.0

# Password hashing (cross-platform alternative to crypt module)
passlib[bcrypt]>=1.7.4

# DNS queries with custom DNS server
dnspython>=2.4.0
